<Window x:Class="Ticky.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Ticky"
        mc:Ignorable="d"
        Title="MainWindow" Height="105" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="3*"></RowDefinition>
            <RowDefinition Height="1*"></RowDefinition>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="5*"></ColumnDefinition>
                <ColumnDefinition Width="2*"></ColumnDefinition>
                <ColumnDefinition Width="2*"></ColumnDefinition>
                <ColumnDefinition Width="2*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" FontSize="32" Text="{Binding Time}"></TextBlock>
            <Button Grid.Column="1" Command="{Binding StartCommand}" Content="Start"></Button>
            <Button Grid.Column="2" Command="{Binding PauseCommand}" Content="Pause"></Button>
            <Button Grid.Column="3" Command="{Binding StopCommand}" Content="Stop"></Button>
        </Grid>
        <TextBlock Grid.Row="1" Text="{Binding FooterText}"></TextBlock>
    </Grid>
</Window>
